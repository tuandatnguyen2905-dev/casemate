CASEMATE SOURCE EXPORT
Generated: 2026-09-13 UTC

This folder contains one ZIP per workspace-root source entry. The outer casemate-source.zip archive packages this folder as a single downloadable artifact. Extract the outer archive, then extract the component ZIPs to reconstruct the source tree.

Included: app source, shared components, hooks, libraries, data, agent prompts, tools, research, config and branding files, generated theme files, integration references, published bundle, repair snapshots, a WorkspaceDB schema manifest, and a server-function registry manifest.

Excluded: node_modules and .next build artifacts (none were present). Platform-managed .cache and .published-source paths cannot be exported by the workspace archive service. Runtime database rows and credentials are not included.
